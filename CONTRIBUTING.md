# Contributing

Keep the core provider-agnostic. Provider SDKs belong in optional integrations.
Preserve cancellation semantics and generation IDs.
Add tests for concurrency, disconnects, stale audio, and provider failures.
Run `pytest` before submitting changes.
